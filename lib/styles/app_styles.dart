import 'package:flutter/widgets.dart';

class AppStyles {
  // Define standard icon sizes
  static const double iconSmall = 24;

  // Define standard button size
  static const double buttonSize = 52;

  // Define standard padding
  static const double horizontalPadding = 18;
  static const double verticalPadding = 24;

  static const double cardBorderPadding = 8;

  // Define standard colors
  static const Color regularTextColor = Color.fromRGBO(26, 26, 26, 1);
  static const Color secondaryTextColor = Color.fromRGBO(153, 153, 153, 1);
  
}