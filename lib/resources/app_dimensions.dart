import 'package:flutter/rendering.dart';

class AppRadius {
  // static final BorderRadius small = BorderRadius.circular(6);
  static final BorderRadius small = BorderRadius.circular(8);
  static final BorderRadius medium = BorderRadius.circular(10);
}